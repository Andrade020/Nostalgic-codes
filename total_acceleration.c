#include<stdio.h>
#include<math.h>

int main ()
{
    float x, y, z, a;
    float at = 0;
    float n; //n sera escrito em todo lugar que eu nao souber o que deveria escrever
    while (1)
    {
        x = n;//fazer transferencia de valores
        y = n;//fazer transferencia de valores
        z = n;//fazer transferencia de valores
        a = sqrt(x*x+y*y+z*z);
        at = at + a;
        //salvar o valor na RAM
    }
}
